package views;

public class StatisticsView extends BaseView {

    public void displayStatisticsMenu() {
        displayMessage("=== STATISTICS ===");
        displayMessage("1. Daily Fuel Usage");
        displayMessage("2. Weight of each type of container");
        displayMessage("3. List ships in a port");
        displayMessage("4. List trips in a day");
        // Continue with further options as needed
    }

    // Other statistics-related views and methods
}
